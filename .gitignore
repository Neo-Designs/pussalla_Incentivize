# Dependencies
**/node_modules/

# Build output (kept in git so the frontend bundle is always available;
# pussalla-backend/public/ is the deploy artifact served by the backend)

# Environment & secrets (NEVER commit real credentials)
.env
pussalla-backend/.env
*.env
*.local

# Test/dev tooling artifacts
pussalla-backend/pythagora-core/
.agents_tmp/
.agents/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
