# Copy this file to .env and fill in real values before running the server.

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=pussalla
PGUSER=pussalla_app
PGPASSWORD=change_me

# Or, alternatively, a single connection string (takes priority if set)
DATABASE_URL=postgresql://neondb_owner:npg_EfylY2Ne4LRF@ep-holy-frost-azs3edyk.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Auth
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=8h

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Seed script: default password assigned to every seeded employee login
SEED_DEFAULT_PASSWORD=Pussalla@123
